# 工程约束

后续 agents 修改工程时请继续维护本文件及各组件自己的 `AGENTS.md`。

- 允许 `cargo fmt` 带来的其他无关文件格式改动。
- 不要编写只使用一次的工具函数。
- 不运行全量测试，只验证实际改动的组件。
- 工程取舍记录在各组件自己的 `AGENTS.md`。

# 根包取舍

- 根包只承载 `src/main.rs` 和 `src/bin` 下的仓库内二进制，不提供 `src/lib.rs` 聚合接口。
- 根目录二进制必须直接依赖 data、scoring、backtest 等能力所有者，禁止恢复 `lianghua_rs::*` 兼容导出。
- 只运行 `cargo check -p lianghua-rs` 以及实际改动二进制的测试。
- `chip_behavior_sample` 固定使用 120 个交易日预热和 1% 等比分桶，并从每只股票可计算的最早日期连续演化状态，与新筹码引擎默认口径保持一致且避免报告截取起点改变结果；它是研究口径，不代表参数已经完成统计寻优。
- `docs/筹码博弈策略.toml` 对高位接力只给 0.20 的低权重，不设置筹码分数硬下限：目的是承认新资金接力的不确定性并缓解归零饱和，同时保留高位派发的主要方向；调整该值后必须复跑常规与反差两组样本。
- `chip_posterior_research` 只做确定性抽样与有限参数对照，逐股缓存放在 `/tmp/lianghua-posterior`，不能为了重复评估交易规则重新全量算筹码。3 根确认、35% 修正是训练期相对优胜的研究候选，验证与检验未证明稳定增益；不得把检验期最优组反向写成训练选择。
- 综合后验对照固定使用第一轮的 3 根 / 35%，单配置模式复用已算的基线；资金/指数硬过滤的训练改善未在新增股票上稳定延续，不能将这种探索结果标成独立时间检验或默认生产策略。
- 生效 `score_rule.toml` 依相关性研究做了激进去噪：固定频率序、25 次随机顺序平均与滚动窗口共同显示 `横有多长组合` 是唯一时间稳健且近正交的因子、`主力持仓变化` 是唯一顺序稳健的增量因子，而 `价格回撤位置`、`换手率组合`、`资金面背离组合` 无独立样本外增量，`brick连续上涨后跌破`、`低开多` 在 25 次随机顺序中增量恒为负；处理方式最初是把这些规则的 `points`/`points_by_hits`/`bonus_points` 归零，但运行中的 app 校验拒绝组合规则的 `points_by_hits` 与 condition bonus 同时全为 0（原 `波动与成交活跃度` 之所以合法是因为它 `points_by_hits` 全 0 而 bonus 非零），因此最终把这 5 条规则整体注释停用；停用前原件备份在同目录 `score_rule.toml.agent-backup-*`，其中 `-v2` 是分数归零但被 app 拒绝的版本。检验段共同上行漂移会给负分规则带来符号偏置，但同类负分规则并非一致为负（放量新高诱多、换手过高、背离量能均为正），因此停用成立；本次样本已参与选择，必须重算评分并用新增日期前向复核后才能恢复或提高权重。
- 上述停用重算后的前后对比（`scripts/dim_research.py`，随机顺序 25 次）：保留规则的全期收益 t 完全不变，说明改动只作用于被停用因子的分数；`主力成本位置` 平均被解释方差 0.352→0.222、`波动与成交活跃度` 0.275→0.218，低吸簇重复结构下降。四个核心因子的随机顺序增量基本保持（`横有多长组合` 1.62→1.69、`主力持仓变化` 2.23→2.10、`波动与成交活跃度` 1.82、`主力成本位置` 1.89），但弱规则的增量明显回落（`放量新高诱多` 0.84→0.17、`背离量能` 0.45→-0.11、`上冲诱多发套` 0.13→-0.23、`磨叽` 0.11→-0.06），说明被停用因子曾是这些弱规则前缀的一部分。前后随机顺序候选规则数由 19 变为 14，对比不完全可控，只有新增日期的前向结果才能决定是否继续裁剪这批弱规则。
